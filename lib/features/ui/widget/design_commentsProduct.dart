import 'package:flutter/material.dart';

class CommentsUserProduct extends StatelessWidget {
  const CommentsUserProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(children: [
        Row(
          children: [],
        )
      ]),
    );
  }
}
